<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ssafy.user.model.mapper.UserMapper">

	<resultMap type="userDto" id="user">
		<result column="user_id" property="user_id" />
		<result column="user_name" property="user_name" />
		<result column="user_pwd" property="user_pwd" />
		<result column="user_email_id" property="user_email_id" />
		<result column="user_email_domain" property="user_email_domain" />
	</resultMap>

	<!-- Admin -->
	<resultMap type="userDto" id="userAll" extends="user">
		<result column="user_join_date" property="user_join_date" />
		<result column="user_status" property="user_status" />
	</resultMap>

	<select id="idCheck" parameterType="String" resultType="int">
		select
		count(user_id)
		from users
		where user_id = #{user_id}
	</select>

	<insert id="joinUser" parameterType="userDto">
		insert into users
		(user_name, user_id, user_pwd, user_email_id, user_email_domain)
		values (#{user_name}, #{user_id}, #{user_pwd}, #{user_email_id},
		#{user_email_domain})
	</insert>

	<select id="loginUser" parameterType="map" resultMap="user">
		select
		user_id, user_name, user_pwd, user_email_id, user_email_domain
		from users
		where
		user_id = #{user_id} and user_pwd = #{user_pwd}
	</select>

	<!-- <select id="findPassword" parameterType="String" resultType="String"> 
		select user_pwd from users where user_name = #{user_name} and user_id = #{user_id} 
		</select> -->

	<select id="listUser" resultMap="userAll">
		select user_name, user_id,
		user_pwd, user_email_id, user_email_domain, user_join_date, user_status
		from users
		order by
		user_join_date
	</select>

	<select id="getUser" parameterType="String" resultMap="userAll">
		select
		user_id, user_name, user_pwd, user_email_id, user_email_domain, user_join_date, user_status
		from users
		where user_id = #{user_id}
	</select>

	<update id="updateUser" parameterType="userDto">
		update users
		<trim prefix="set" suffixOverrides=",">
			<if test="user_pwd != null">user_pwd = #{user_pwd},</if>
			<if test="user_email_id != null">user_email_id = #{user_email_id},</if>
			<if test="user_email_domain != null">user_email_domain = #{user_email_domain},</if>
		</trim>
		where user_id = #{user_id}
	</update>

	<delete id="deleteUser" parameterType="String">
		delete from users
		where
		user_id = #{user_id}
	</delete>
</mapper>